#include "conceptconstexpr.hpp"

namespace sp {

// constexpr auto getsize(const auto &t) { return t.size(); }

// void send_oneping(size_check<1> auto &cont) { std::puts("sending one ping");
// }
} // namespace sp
